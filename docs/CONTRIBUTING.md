# Contributing

The contribution process follows these rules:

- All contributions must come through a **fork → pull request** workflow.  
- Accepted contributions include **bug fixes, new features, and general improvements**.  
- All PRs must target the `main` branch and will be reviewed before merging.